$kind: http-request
name: 'Pin/Unpin Movie to Homepage'
method: PATCH
url: '{{base_url}}/admin/movies/{{movie_id}}/pin'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
body:
  type: json
  content: |-
    {
      "is_pinned": true
    }
