$kind: http-request
name: Admin Change User Role
method: PATCH
url: '{{base_url}}/admin/users/{{user_id}}/role'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
body:
  type: json
  content: |-
    {
      "role": "vip"
    }
