$kind: http-request
name: 'Admin Lock/Unlock User'
method: PATCH
url: '{{base_url}}/admin/users/{{user_id}}/status'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
body:
  type: json
  content: |-
    {
      "status": "locked"
    }
