$kind: http-request
name: Sync Movies from TMDB
method: POST
url: '{{base_url}}/admin/tmdb/sync-movies'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
body:
  type: json
  content: |-
    {
      "category": "popular",
      "page": 1
    }
