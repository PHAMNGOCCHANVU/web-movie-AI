$kind: http-request
name: Get Profile
method: GET
url: '{{base_url}}/user/profile'
auth:
  type: bearer
  credentials:
    - key: token
      value: '{{token}}'
description: |
  Đồng bộ subscription với /auth/me (plan_code, status, expires_at).
